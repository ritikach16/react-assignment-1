import React, { FC } from "react";
import "./Header.css";

const Header: FC = () => {
  return (
    <div className="header">
        <h1 className="center white">Todo List</h1>
    </div>
  );
};

export default Header;
